---
- name: user
  hosts: demo
  # vars_prompt:
  #   - name: user
  #     prompt: "Введите пользователя"
  #     private: no
  # vars_files:
  #   - ./my_vars.yml 
  tasks: 
    - name: Create user
      vars:
        - user: alaricode
      user:
        name: "{{ user }}"
        state: present
      become: true
      debugger: always
    #   register: out
    # - debug: 
    #     var: out